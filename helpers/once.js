function _once(fn) {
  // _once should return a function that will only run its logic on the first invocation.
  // The next time it is invoked, it will simply return the original return value.

  // Hints: look up "javascript this"
  // call, apply, and bind methods.
  // function scope

  // CODE HERE


}

module.exports = _once;